# vim: ft=zsh

. "$HOME/.zsh/config"
. "$HOME/.zsh/aliases"
. "$HOME/.zsh/completion"
. "$HOME/.zsh/prompt"
. "$HOME/.zsh/bindings"
. "$HOME/.zsh/tmux.plugin.zsh"

[ -f "$HOME/.zsh/local" ] && . "$HOME/.zsh/local"
